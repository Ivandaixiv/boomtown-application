import { createStyles } from "@material-ui/styles";
const styles = () =>
  createStyles({
    card: {
      width: 500
    },
    media: {
      height: 250
    },
    text: {
      height: "10rem"
    },
    button: {
      border: "1px solid rgba(0, 0, 0, 0.25)"
    }
  });
export default styles;
