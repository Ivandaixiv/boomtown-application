import { createStyles } from "@material-ui/styles";
const styles = () =>
  createStyles({
    card: {
      width: 425
    },
    media: {
      height: 300
    },
    text: {
      height: "10rem"
    },
    button: {
      border: "1px solid rgba(0, 0, 0, 0.25)"
    }
  });
export default styles;
